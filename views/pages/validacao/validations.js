// const Joi = require('joi')

// const userSchema = Joi.object({
//     username: Joi.string()
//         .alphanum()
//         .min(3)
//         .max(30)
//         .required(),

//     password: Joi.string()
//         .pattern(/^[a-zA-Z0-9]{3,30}$/),

//     birth_year: Joi.number()
//         .integer()
//         .min(1900)
//         .max(2001),

//     email: Joi.string()
//         .email({ minDomainSegments: 2, tlds: { allow: ['com', 'net'] } })
// });

// const valida = 


// module.exports = {
//     userSchema
// }


//**COMANDOS PRIMEIROS CRIANDO REPOSITÓRIO NO GIT**
// - git init
// - git status
// - git add .
// - git status
// - git commit -m "comentário" 
// (Criar a pasta do projeto no git e depois copiar o endereço link https)
// - git remote add origin 'https://github.com/usuario/nomedop...'
// - git remote show origin -n
// - git remorte rm ( comando para remover repositório e reiniciar)
// - git push origin master


// **COMANDOS ATUALIZANDO REPOSITÓRIOS**
// - git status  
// - git add .
// - git status
// - git commit -m "mensagem"
// - git push origin master // não funcionando o master faz o head